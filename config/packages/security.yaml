security:
    # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
    providers:
        admin_provider:
            memory:
                users:
                    admin: {password: '$2y$13$UfEOu6T9jwwqOX4IayvDw.5mA4/7IP1zMSPVNU2JFiQQpOiUnjd02', roles: ['ROLE_ADMIN']}
                    god: {password: '$2y$13$.YdAlNYuqxu5UAz2Qr8EP.EiYdU4dFRnL8xZLx3I9lLh6jMJDkkhW', roles: ['ROLE_GOD']}
        author_provider:
            entity :
                class: 'App\Entity\Author'
                property: 'email'
    encoders:
        App\Entity\Author: 'auto'
        Symfony\Component\Security\Core\User\User:
            algorithm : bcrypt
    role_hierarchy:
        ROLE_GOD: [ROLE_ADMIN, ROLE_ALLOWED_TO_SWITCH]
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        login:
            pattern: ^/login
            anonymous: true
        admin:
            pattern: ^/admin
            anonymous: false
            provider: admin_provider
            switch_user: true
            form_login:
                login_path: admin-login
                check_path: admin-login-check
                default_target_path: admin_index
        main:
            pattern: ^/
            anonymous: lazy
            provider: author_provider
            switch_user: true
            form_login:
                login_path: security-login
                check_path: author-login-check
                default_target_path: article-list
            logout:
                path: logout
                target: homepage

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#firewalls-authentication

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        - {path: ^/admin/heaven, roles: ROLE_GOD}
        - {path: '^/admin', roles: 'ROLE_ADMIN'}
        # - { path: ^/admin, roles: ROLE_ADMIN }
        # - { path: ^/profile, roles: ROLE_USER }
